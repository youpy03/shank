<p id="notice"><%= notice %></p>

<h1>LatPullDown Home画面</h1>

<table>
  <thead>
    <tr>
      <th>①User :id</th>
      <th>②Title</th>
      <th>③Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <td><%= link_to '詳細', post %></td>
        <td><%= link_to '編集', edit_post_path(post) %></td>
        <td><%= link_to '削除', post, method: :delete, data: { confirm: 'この投稿を削除してもいいですか??' } %>
        </td>
      </tr>
    <% end %>
    <p>#タスク編集画面(edit)の完成をすること</p>
    <p>#タスクfav れるようにすること</p>
    <p>#デザインコンセプトを考えてくること</p>

  </tbody>
</table>

<br>

<%= link_to '新規投稿', new_post_path %>
